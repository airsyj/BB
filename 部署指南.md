# 市政工程进场出场报备系统 · 免费线上部署指南

本系统是一个**纯 Node.js + 原生前端**的报备应用，无需数据库、无需构建步骤。
任何人用手机或电脑打开网址即可填报，数据全部留存，提交前自动校验必填项，并按身份证号/手机号/车牌自动识别「新办 / 续期」并回填历史信息。

本指南帮助你在**免费的 Node 托管平台**上部署，部署后**不需要你一直开着电脑**。

---

## 一、两种免费托管方案（任选其一）

| 平台 | 免费额度 | 优点 | 注意 |
|------|----------|------|------|
| **Render** (Web Service) | 免费档，实例闲置 15 分钟后休眠，再次访问自动唤醒（首访较慢） | 配置最简单，GitHub 一键连 | 免费档**本地磁盘不持久**，重新部署后数据重置（见第六节） |
| **Koyeb** (Serverless / App) | 免费档含持久存储卷 | 磁盘可持久，数据不易丢 | 首次配置略多一步（挂存储卷） |

> 两者都需要你先注册一个免费账号（用 GitHub 或邮箱即可，无需付费）。

---

## 二、准备代码仓库（只需做一次）

仓库里已经包含：

- `server.js` / `lib/` —— 后端
- `public/` —— 前端页面（手机/电脑自适应）
- `package.json` —— 已声明依赖 `exceljs` 与启动命令 `npm start`
- `config.example.json` —— **已脱敏的配置模板**（已入库，**不含任何密钥**）
- `.gitignore` —— 已忽略 `config.json`（你的本地真实密钥）、`data/`、`uploads/`

> 安全说明：`config.json` 仅存在于你本机，不会被推送到任何仓库。线上密钥一律通过**环境变量**注入，绝不明文入库。

将本目录推送到你自己的 GitHub 仓库：

```bash
# 在 report-system 目录下执行（仓库已 git init 并做了首次提交）
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

如果你还没创建 GitHub 仓库：登录 github.com → New repository → 取名（如 `report-system`）→ 创建（**不要**勾选自动生成 README）→ 复制地址填到上面的 `git remote add origin`。

---

## 三、部署到 Render（推荐，最简单）

1. 打开 https://render.com → 注册/登录。
2. 右上角 **New** → **Web Service**。
3. 选择 **Connect a repository** → 授权 GitHub → 选中你的 `report-system` 仓库。
4. 配置：
   - **Name**：`report-system`（任意）
   - **Runtime**：`Node`
   - **Build Command**：`npm install`
   - **Start Command**：`npm start`
   - **Instance Type**：选择 **Free**（免费）
5. 展开 **Advanced → Environment Variables**，按第四节填写密钥（至少设置 `ADMIN_TOKEN`）。
6. 点击 **Create Web Service**。
7. 等待构建完成（约 1–2 分钟），Render 会分配一个形如 `https://report-system-xxxx.onrender.com` 的公开网址。
8. 把这个网址发给同事/工人，手机电脑都能直接打开填报。

> 免费档实例在闲置 15 分钟后会休眠，下一次访问需要几秒唤醒，属正常现象。

---

## 四、部署到 Koyeb（磁盘可持久）

1. 打开 https://www.koyeb.com → 注册/登录。
2. **Create App** → 选择 **GitHub** 仓库 `report-system`。
3. 构建/运行设置：
   - **Build command**：`npm install`
   - **Run command**：`npm start`
   - **Port**：`3000`
4. 在 **Volumes / Store** 处挂一个免费存储卷（例如挂载到 `/app/data` 与 `/app/uploads`），让报备数据持久保存而不随重启丢失。
5. 在 **Environment variables** 中按第四节填写（至少 `ADMIN_TOKEN`）。
6. 部署完成后获得公开网址，即可分享使用。

---

## 五、环境变量说明（在托管平台的后台填写）

| 变量名 | 是否必填 | 作用 |
|--------|----------|------|
| `ADMIN_TOKEN` | **强烈建议** | 后台管理 `/admin` 的访问口令。公开部署务必设为**强随机字符串**，否则后台可被他人进入。 |
| `PORT` | 否 | 端口，平台一般自动注入；默认 3000。 |
| `SILICONFLOW_KEY` | 否 | 硅基流动（SiliconFlow）大模型 OCR 密钥。填了证件拍照即可自动识别；不填则降级为手动填写，不影响其他功能。 |
| `HUNYUAN_SECRET_ID` / `HUNYUAN_SECRET_KEY` | 否 | 腾讯混元视觉识别备用密钥（同样用于证件 OCR），可不填。 |

> 不填 OCR 密钥时，系统会自动降级为「手动填写」，核心报备、自动续期识别、校验、导出全部正常。

---

## 六、重要提醒：数据持久化

- **本地运行**：数据存在 `report-system/data/reports.json`，永久保留。
- **Render 免费档**：实例磁盘是临时的，**重新部署（redeploy）后数据会重置**。日常填报在实例存活期间正常保存；建议：
  - 定期进入 `/admin`（填 `ADMIN_TOKEN`）点「导出 Excel」做备份；或
  - 升级到带持久磁盘的方案（Render Disk / Koyeb 卷，见上）。
- **Koyeb 免费档**：挂了存储卷后数据可跨重启保留。
- 不管哪种方案，**重要数据都建议定期导出 Excel 归档**。

---

## 七、本地运行（可选，调试用）

```bash
cd report-system
npm install
npm start
# 打开 http://localhost:3000
```

后台管理：http://localhost:3000/admin （本地默认 token：`admin123`，线上请用 `ADMIN_TOKEN` 覆盖）

---

## 八、功能清单（部署后即可用）

1. **信息全部留存 + 自动新办/续期识别**：每次填报时，输入身份证号、手机号或车牌，系统自动检索历史；命中则标记为「续期」并回填姓名、证件、车辆等信息，未命中标记「新办」；所有字段均可手动修改。
2. **手机/电脑线上使用**：部署后获得公开网址，任何设备浏览器直接打开即可，无需你开机。
3. **提交前校验**：确认提交前弹出核对框，必填项（证件号码、姓名、手机号、是否携带笔记本等）漏填、身份证校验位错误、车牌格式错误、手机号非 11 位、勾选「携带笔记本」却未填型号等，都会**阻止提交并提示具体错误**。
4. **后台管理**：按日期查看全部报备、一键导出 Excel（与甲方模板结构一致）。
