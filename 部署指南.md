# 北京恒基西安报备系统 · 线上部署指南（腾讯云 CloudBase 云托管）

本系统是一个**纯 Node.js + 原生前端**的报备应用，无需自建数据库。
任何人用手机或电脑打开网址即可填报，数据全部留存，提交前自动校验必填项，并按身份证号/手机号/车牌自动识别「新办 / 续期」并回填历史信息。

> **为什么选 CloudBase 云托管**：Render / Koyeb 等境外平台在国内访问常需 VPN、易卡顿；而腾讯云 CloudBase **国内访问快、免 VPN、免你开电脑**，且环境每月有 **720 核·小时免费量**（1 核 1G 约等于白跑 30 天），计算基本免费；数据通过挂载 **CFS 文件存储**持久化，重启不丢。

---

## 一、你需要准备的（一次性）

1. **腾讯云账号 + 实名认证**（国内云厂商硬性要求，微信/手机号即可完成，这一步需你本人操作）。
2. 本 `report-system` 目录已 `git init` 并提交；把它推到你自己的 GitHub（下面有命令）。
3. 一个**已备案的域名**（可选）：云托管会分配一个「默认域名」仅供测试；**正式给工人用，建议绑定你自己已备案的域名**（国内未备案域名无法访问）。若暂无备案域名，可先用默认域名试用。

---

## 二、准备代码仓库（只需做一次）

仓库已包含：`server.js` / `lib/`（后端）、`public/`（前端）、`package.json`（依赖 `exceljs` + 启动命令 `npm start`）、`Dockerfile`、`config.example.json`（已脱敏模板，不含密钥）、`.gitignore`（已忽略 `config.json`、`data/`、`uploads/`）。

> 安全：`config.json` 只在本机，不会入库；线上密钥一律用**环境变量**注入。

推送到你的 GitHub：

```bash
# 在 report-system 目录下
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

（没仓库就去 github.com → New repository → 取名 → 不勾选 README → 复制地址。）

---

## 三、在腾讯云创建环境并开通云托管

1. 打开 https://tcb.cloud.tencent.com → 登录 → **新建环境**（选有免费量的按量/套餐环境，如「基础版 1」或按量计费，环境名任意，记住**环境 ID**）。
2. 左侧进入 **云托管** → 点击 **开通云托管**（按提示开通，默认进入「系统默认仓库」即可）。
3. 点击 **新建服务**，填写服务名称（如 `report-system`），提交。

---

## 四、新建版本并部署（关键步骤）

在刚建好的服务里点击 **新建版本**，按以下配置：

- **上传方式**：选「**代码库导入**」→ 授权并选择你的 GitHub 仓库 `report-system`
  （若不想连 GitHub，也可选「**本地代码上传**」直接把 `report-system` 文件夹压缩上传）
- **监听端口**：填 **`3000`**（与 `Dockerfile` 中一致；服务调用方无需感知此端口）
- **构建命令**：`npm install`
- **启动命令**：`npm start`
- **流量策略**：先保持「部署后流量 0」，稍后手动配 100%

点击 **开始部署**，等待状态变为「正常」。

---

## 五、配置环境变量（控制台「版本」→「环境变量」）

| 变量名 | 是否必填 | 作用 |
|--------|----------|------|
| `ADMIN_TOKEN` | **必填** | 后台 `/admin` 口令。**务必设为强随机字符串**（如 `openssl rand -hex 16` 生成），否则后台可被他人进入。 |
| `DATA_DIR` | **必填（配合 CFS）** | 数据文件目录，填 **`/data`**（指向挂载的 CFS）。 |
| `UPLOADS_DIR` | **必填（配合 CFS）** | 上传照片目录，填 **`/data/uploads`**（同上，照片才能持久化）。 |
| `PORT` | 否 | 端口，云托管会用「监听端口」3000；留空即可。 |
| `SILICONFLOW_KEY` | 否 | 硅基流动大模型 OCR 密钥。填了证件拍照可自动识别；不填则降级手动填写，不影响其他功能。 |
| `HUNYUAN_SECRET_ID` / `HUNYUAN_SECRET_KEY` | 否 | 腾讯混元视觉识别备用 OCR 密钥，可不填。 |

> 不填 OCR 密钥也能正常报备、自动续期、校验、导出。

---

## 六、挂载 CFS 持久化（让数据重启不丢）

云托管容器是无状态的，必须挂载 CFS 文件存储才能持久保存数据：

1. 进入**腾讯云控制台 → 文件存储 CFS**，在**与云开发环境相同地域**（如上海）创建文件系统，并建立一个挂载点（按提示选 VPC / 子网）。
2. 回到云托管服务的 **服务详情**：
   - 开启 **私有网络**，选择与 CFS 相同的 VPC / 子网；
   - 开启 **存储挂载**，存储类型选「文件存储 CFS」，选择上面建的文件系统与挂载点，**挂载路径填 `/data`**，权限「读写」。
3. 保存后，容器里的 `/data` 就是持久化目录；配合第五节 `DATA_DIR=/data`、`UPLOADS_DIR=/data/uploads`，报备数据与照片都会落在 CFS 上。

> 费用：CFS 按存储量计费，几 MB~几十 MB 数据每月仅约几毛钱，几乎可忽略。环境本身有免费额度。

---

## 七、配置流量并拿到访问地址

1. 服务详情 → **流量配置**，把刚部署的版本流量设为 **100%**。
2. 点击 **访问服务**，即可获得一个形如 `https://xxx.ap-shanghai.run.tcloudbase.com` 的**默认域名**（仅测试用，不承诺 SLA）。
3. **正式使用**：在「服务配置 → 自定义域名」绑定你**已备案的域名**（HTTPS 需上传证书）。未备案域名国内无法访问。
4. 把这个网址发给同事/工人，手机电脑浏览器直接打开即可填报。

---

## 八、迁移本地历史数据（可选）

本机已积累的 147 条报备，可一次性推到云端实例（云端会按导入时刻重新生成编号与时间；自动续期检索按身份证/手机号/车牌匹配，不受影响）：

```bash
cd report-system
node migrate.js https://你的云端域名 你的ADMIN_TOKEN
```

---

## 九、功能清单（部署后即可用）

1. **信息全部留存 + 自动新办/续期识别**：填报时输入身份证号/手机号/车牌，自动检索历史；命中标记「续期」并回填，未命中标记「新办」；字段均可手改。
2. **手机/电脑线上填报**：获得国内可直连的网址，任何设备浏览器打开即用，无需你开机。
3. **提交前校验**：确认框核对；必填项漏填、身份证校验位错、车牌格式错、手机号非 11 位、勾选「携带笔记本」却未填型号等，均**阻止提交并提示具体错误**；录入时还会实时检测「不同人使用相同身份证号/手机号」的冒用风险。
4. **管理员通知**：超级管理员在 `/admin` 发布通知，首页顶部醒目横幅展示。
5. **我的报备**：凭本人身份证号+手机号查看并**仅可修改本人**记录（服务端校验归属）。
6. **后台管理**：按日期查看全部报备、一键导出 Excel（与甲方模板结构一致）。

---

## 十、本地运行（调试用）

```bash
cd report-system
npm install
npm start
# 打开 http://localhost:3000
```

后台：http://localhost:3000/admin （本地默认 token `admin123`，线上用 `ADMIN_TOKEN` 覆盖）。
本地数据在 `data/`，照片在 `uploads/`；若想模拟云上持久化，可 `DATA_DIR=/tmp/cb UPLOADS_DIR=/tmp/cb/uploads npm start`。
